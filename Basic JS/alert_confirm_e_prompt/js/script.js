// Exercício:

const num1 = prompt("Digite um número");
const num2 = prompt("Digite outro número");

const resultado = Number(num1) + Number(num2);

alert(`O resultado da soma é: ${resultado}`);

// alert("Com a nossa mensagem");

// Anotações:
// - O objeto window representa tudo que está na janela do navegador (Ex.: os alertas);
// - confirm(msg): abre um alerta com os botões Ok e cancelar, retornando
// respectivamente true ou false;
// - prompt(msg): abre um alerta com uma área de input, retornando o valor recebido;/
// - Obs.: prompt sempre retorna uma string;

